let saludo = "hola".indexOf('l')
//console.log(saludo)

let numbers =[22,33,54,66,72]
//console.log(numbers[numbers.length])

let peliculas = ["star wars", "totoro", "rocky", "pulp fiction", "la vida es bella"]

function TodoAMayusculas(peliculas){
    let peliculasstring = peliculas.join();
    return peliculasstring.toUpperCase()
}

peliculas = (TodoAMayusculas(peliculas)).split(",")

//console.log(peliculas)


const asiaScores = [8, 10, 6, 9, 10, 6, 6, 8, 4];
const euroScores = [8, 10, 6, 8, 10, 6, 7, 9, 5];
let resultados = []

function comparado2 (a , b) {
    for (let i = 0; i < a.length; i++) {
    const nombrepelis = a[i];
    console.log(nombrepelis)
}
}
